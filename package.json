{
  "name": "codegpt-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && set PORT=3000 && react-scripts start && PORT=8000 react-scripts start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7"
  }
}
